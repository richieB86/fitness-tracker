// Der komplette React-Code aus dem vorherigen Fitness-Tracker-Artifact hier
// Konvertiert in vanilla JavaScript für die Browser-Ausführung

'use strict';

const { useState, useEffect } = React;
const { Plus, Save, BarChart, Dumbbell, AlertCircle } = window.LucideReact;
const { LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, Legend } = Recharts;

function FitnessTracker() {
    // Der gesamte React-Komponenten-Code hier...
    // (Der vollständige Code wurde aus Platzgründen gekürzt - er ist identisch mit dem vorherigen React-Komponenten-Code)
}

const container = document.getElementById('root');
ReactDOM.render(React.createElement(FitnessTracker), container);
